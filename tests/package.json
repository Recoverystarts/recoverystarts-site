{
  "name": "recoverystarts-bigbook-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dev-only VERIFY harness for the ported Big Book search engine. Not part of the deployed site. Run: cd tests && npm install && npm test",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
